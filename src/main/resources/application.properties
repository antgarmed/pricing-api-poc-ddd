# Spring
spring.application.name=poc-prices-api
spring.output.ansi.enabled=always

# DataSource
spring.datasource.url=jdbc:h2:mem:pricing;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;INIT=SET TIME ZONE 'UTC'
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=
spring.datasource.password=

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.open-in-view=false

# SQL init
spring.sql.init.mode=always

# 2 Console
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.url=docs/openapi.yaml
